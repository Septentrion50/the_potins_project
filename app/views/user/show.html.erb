<%u = User.find(params[:id])%>
<div class="container">
  <div class="container-fluid mt-5">
    <h1><%=u.first_name%> <%=u.last_name%></h1>
    <h3 class="text-muted"><%=u.age%> ans, de <%=u.city.name%></h3>
    <p><%=u.description%></p>
  </div>
</div>
